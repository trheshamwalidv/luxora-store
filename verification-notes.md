# Verification Notes

تمت معاينة الواجهة العامة على سطح المكتب، وكذلك صفحة المتجر وصفحة منتج تجريبي. يعمل التسلسل البصري للحملة ثم المجموعات ثم المنتجات بصورة متماسكة، وتظهر صور الحملة والمنتج الأصلية في المواضع المقصودة. كما تعرض واجهة المتجر الفلاتر والبحث، وتعرض صفحة المنتج السعر وخيارات اللون والمقاس قبل الإضافة للسلة.

تمت إضافة صفحة عروض مستقلة، وحالة مخزون صريحة في صفحة المنتج، وأدوات إدارة المقاسات والألوان والكميات وروابط صور المعرض لكل منتج في لوحة التحكم. تظهر المعاينات على شاشات الهاتف وسطح المكتب بصورة متجاوبة.

تم تشغيل فحص TypeScript، واختبارات Vitest، وبناء الإنتاج بنجاح. مرّت خمس اختبارات: اختبار تسجيل الخروج المدمج، وثلاثة اختبارات لسلامة الكتالوج، واختبار واحد يضمن حجب الدفع حتى يتم ربط متجر Shopify حقيقي.

يبقى الربط الفعلي مع Shopify متعمدًا كخطوة لاحقة، لأنه يحتاج إلى متجر Shopify حقيقي تختاره أنت وحساباته؛ لم يتم إنشاء متجر تجريبي أو الاتصال بأي حساب خارجي.

تم التحقق من أن مسار `/admin` يعرض بوابة تسجيل دخول قبل إتاحة اللوحة. تعذر إتمام تسجيل دخول المدير داخل متصفح الاختبار لأن بوابة المصادقة الخارجية أعادت رسالة عدم توفر في المنطقة؛ لذلك لم يتم إدخال أو تعديل أي بيانات حقيقية أثناء الاختبار. المسار والواجهة والحماية موجودة، وسيكتمل اختبار التعديل المحمي عند فتحها من جلسة المالك المتاحة في بيئة الإدارة.

تم فتح صفحة منتج `Vector One` في المعاينة والتحقق من ظهور خيارات اللون والمقاسات 40–43، وحالة المخزون الإجمالية قبل اختيار مقاس، مع تعطيل المقاس غير المتاح. الخطوة التالية في التحقق هي اختبار إضافة مقاس متاح إلى السلة وحد الكمية داخلها.

تم اختيار المقاس 42، وظهرت رسالة "متبقي 3 فقط من هذا المقاس". ثم أُضيف المنتج إلى السلة بنجاح؛ ظهر عداد السلة، والسطر الصحيح للمنتج واللون والمقاس والسعر، مع رسالة توضيحية بأن إتمام الدفع الحقيقي سيُفعّل فقط بعد ربط Shopify.

تمت زيادة الكمية داخل السلة حتى وصلت إلى 3، وهو الحد المتاح للمقاس 42. عندها ظهرت رسالة على سطر المنتج ورسالة تنبيه عامة في السلة توضح بلوغ الحد الأقصى، وتوقف زر الزيادة. بذلك أصبحت حالة نفاد/حد المخزون واضحة داخل صفحة المنتج والسلة معًا.

بعد تفعيل Shopify، أكد فحص Storefront API وصول منتجين رسميين منشورين مع عنوان وصورة وسعر. تعرض صفحة `/shop` هذين المنتجين من Shopify مباشرة، مع الصورة والسعر والفئة، بينما يبقى الكتالوج المحلي احتياطيًا إذا لم يتوفر اتصال التجارة.

تم فتح منتج Shopify "Vector One" من الكتالوج، واختيار متغيره المتاح، ثم إضافته بنجاح. ظهرت رسالة النجاح من مسار السلة الرسمي، ما يؤكد أن واجهة المنتج تستدعي سلة Shopify بدل السلة الاحتياطية المحلية عند توفر منتجات Shopify.

تمت إعادة قراءة حالة الصفحة بعد الإضافة. ظهر عداد السلة بقيمة 1، وعُرض المنتج الرسمي والصورة والسعر والإجمالي داخل لوحة سلة Shopify، مع زر إتمام الطلب الآمن. لم يتم تنفيذ أي دفع أو إدخال بيانات دفع خلال التحقق.

تم الضغط على زر إتمام الطلب للتحقق من المسار فقط، ففُتحت صفحة Shopify Checkout ومعها المنتج والسعر والإجمالي الصحيحون. لم يُدخل أي اسم أو عنوان أو بيانات دفع. صفحة الدفع تعرض أن المتجر لا يقبل المدفوعات حاليًا، وهو متوقع حتى تُطالب بملكية المتجر وتضبط مزود الدفع والشحن من إعدادات Shopify.
